apiVersion: v1
kind: Service
metadata:
  name: {{ include "homecraft-frontend.fullname" . }}
  labels:
    {{- include "homecraft-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homecraft-frontend.selectorLabels" . | nindent 4 }}
