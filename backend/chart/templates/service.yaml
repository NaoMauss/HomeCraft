apiVersion: v1
kind: Service
metadata:
  name: {{ include "homecraft-backend.fullname" . }}
  labels:
    {{- include "homecraft-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homecraft-backend.selectorLabels" . | nindent 4 }}
