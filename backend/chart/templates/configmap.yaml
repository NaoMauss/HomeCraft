apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "homecraft-backend.fullname" . }}
  labels:
    {{- include "homecraft-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
